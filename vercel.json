{
  "installCommand": "bun install",
  "buildCommand": "bun run build:shared && bun run build:client",
  "outputDirectory": "client/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
